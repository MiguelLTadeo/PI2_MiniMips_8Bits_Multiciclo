# PI2_MiniMips_8Bits_Multiciclo

git checkout -b nome_da_branch -> Criação de nova branch para implementar a funcionalidade

git branch -> lista a branch que está atualmente e outras disponíveis

git switch nome_da_branch -> troca para uma branch de sua escolha

git pull -> puxa alterações do repositório github

git push -> empurra alterações locais para o repo github

# Comandos para Compilar e Rodar

gcc main.c mips8bit.c -o run

./run